function Sessao({tipo}) {
  return (
    <section className={`Sessao${tipo}`}>
      <h1>Esta é minha tarefa</h1>

      <p>Usando React para criar, exportar e Importar componentes</p>
    </section>
  )
}

export default Sessao;

